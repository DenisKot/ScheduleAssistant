using System;

namespace ScheduleAssistant.Common
{
    public class Constants
    {
        // ToDo: get from session user time zone
        public static readonly TimeSpan TimeZoneOffset = TimeSpan.FromHours(3);
    }
}
